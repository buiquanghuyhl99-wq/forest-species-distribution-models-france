# Outputs folder

Generated figures and tables are saved here when the R workflow runs locally.

The generated outputs are excluded from Git by default. This prevents accidental publication of private or unpublished results.
